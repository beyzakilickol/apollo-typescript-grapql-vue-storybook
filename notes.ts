/**
 * Created by Beyza Kilickol on 2020-03-24
 * new_pz_storefront
 */


// TO GENERATE STATIC TYPES FROM SCHEMAS DEFINED ON THE SERVER:

/**
 * 1. Comment out the service section to be able to run apollo codegen
 * 2. Update the path of includes section depending on where you want to add types
 * For example: ['src/graphql/.....'] in apollo.config.js file
 * 3. Update endpoint and outputFlat values to decide folder name and server url on package.json file
 * "apollo:generate": "apollo codegen:generate --target typescript --excludes=node_modules/*  --endpoint http://localhost:4000/graphql --outputFlat src/graphql/types"
 */
